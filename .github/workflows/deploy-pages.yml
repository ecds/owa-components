name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install

      - name: Build apps
        run: |
          REPO=owa-components
          ORG=ecds
          BASE_URL=https://${ORG}.github.io/${REPO}

          for app in apps/*/; do
            name=$(basename "$app")
            VITE_WP_BASE="${BASE_URL}/apps/${name}/" \
              pnpm --filter "$name" build
          done

      - name: Collect dist folders
        run: |
          mkdir -p _site/apps
          for app in apps/*/; do
            name=$(basename "$app")
            if [ -d "$app/dist" ]; then
              cp -r "$app/dist" "_site/apps/$name"
            fi
          done

      - name: Add index page
        run: |
          cat > _site/index.html << 'EOF'
          <!DOCTYPE html>
          <html lang="en">
          <head>
            <meta charset="UTF-8">
            <title>OWA Components</title>
            <style>
              body { font-family: sans-serif; max-width: 600px; margin: 3rem auto; padding: 0 1rem; }
              ul { line-height: 2; }
            </style>
          </head>
          <body>
            <h1>Open World Atlanta Components</h1>
            <ul id="list"></ul>
            <script>
              const apps = APPS_LIST;
              const ul = document.getElementById('list');
              apps.forEach(name => {
                const li = document.createElement('li');
                li.innerHTML = `<a href="apps/${name}/">${name}</a>`;
                ul.appendChild(li);
              });
            </script>
          </body>
          </html>
          EOF

          # Inject the real app list
          apps=$(ls _site/apps/ | python3 -c "import sys,json; print(json.dumps(sys.stdin.read().split()))")
          sed -i "s|APPS_LIST|${apps}|" _site/index.html

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy-pages:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4

  deploy-wp:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install

      - name: Build apps for WordPress
        run: |
          WP_PLUGIN_BASE=/wp-content/plugins/owa-components

          for app in apps/*/; do
            name=$(basename "$app")
            VITE_WP_BASE="${WP_PLUGIN_BASE}/${name}/" \
              pnpm --filter "$name" build
          done

      - name: Deploy to WordPress server
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan "${{ secrets.WP_HOST }}" >> ~/.ssh/known_hosts

          SSH_OPTS="-i ~/.ssh/id_rsa -o IdentitiesOnly=yes -o StrictHostKeyChecking=no"
          DEST="${{ secrets.WP_SSH_USER }}@${{ secrets.WP_HOST }}:${{ secrets.WP_PLUGIN_PATH }}/owa-components"

          # Deploy the plugin file
          rsync -az -e "ssh ${SSH_OPTS}" owa-components.php "${DEST}/"

          # Deploy each app's built assets
          for app in apps/*/; do
            name=$(basename "$app")
            if [ -d "$app/dist" ]; then
              rsync -az --delete -e "ssh ${SSH_OPTS}" "$app/dist/" "${DEST}/${name}/"
            fi
          done
